//cargo new : create a project
// cargo check to check  if program can compile
//cargo build :  build program executables
// cargo run : build and run a project in single step
// cargo build --release : command will create an executable in target/release instead of target/debug.

fn main() {
    println!("Hello, world!");
}
